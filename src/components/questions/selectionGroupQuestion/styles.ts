import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: { padding: 10 },
  flatListContent: { gap: 10 },
  optionButton: { padding: 10, borderRadius: 5, backgroundColor: '#ddd' },
  selectedOption: { backgroundColor: '#8bc34a' },
  optionText: { fontSize: 16 },
});
